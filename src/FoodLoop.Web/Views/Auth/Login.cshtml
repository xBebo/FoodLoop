@{
    ViewData["Title"] = "Welcome back";
}

<div class="auth-split">
    <partial name="_AuthBrand" />

    <section class="auth-panel" aria-labelledby="auth-title">
        <div class="auth-panel__inner">
            <p class="auth-brandline mb-0">Organization account</p>
            <h1 id="auth-title" class="auth-title">Welcome back</h1>
            <p class="auth-subtitle">Pick up where you left off. Good food is on the move.</p>

            @if (TempData["SuccessMessage"] != null)
            {
                <div class="alert alert-success" role="status">@TempData["SuccessMessage"]</div>
            }

            <form asp-action="Login" method="post" class="auth-form">
                <div asp-validation-summary="All" class="text-danger mb-3"></div>
                @if (ViewData["ReturnUrl"] is string returnUrl && !string.IsNullOrWhiteSpace(returnUrl))
                {
                    <input type="hidden" name="returnUrl" value="@returnUrl" />
                }

                <div class="mb-3">
                    <label class="form-label" for="email">Email address</label>
                    <input id="email" type="email" name="email" class="form-control" autocomplete="email" required />
                </div>

                <div class="mb-4">
                    <label class="form-label" for="password">Password</label>
                    <input id="password" type="password" name="password" class="form-control" autocomplete="current-password" required />
                </div>

                <button type="submit" class="btn btn-primary w-100">Log in</button>
            </form>

            <p class="auth-switch">
                New to FoodLoop?
                <a asp-action="Register" class="fw-semibold">Register an organization</a>
            </p>
        </div>
    </section>
</div>
